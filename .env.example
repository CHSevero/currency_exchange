# Exchange Rate API Settings
EXCHANGE_RATE_API_KEY=your_api_key_here  # Required: Get your API key from exchangeratesapi.io
EXCHANGE_RATE_API_URL=http://api.exchangeratesapi.io/latest  # Optional: Base URL for exchange rate API
EXCHANGE_RATE_BASE_CURRENCY=EUR  # Optional: Base currency for rates (currently only EUR supported in free tier)
EXCHANGE_RATE_CACHE_TTL=3600  # Optional: Cache duration in seconds (default: 1 hour)
